const constant = {
    localStorageKeys: {
        id: "planeb.id",
        name: "planeb.name",
    },
    rooms: {
        dbMetadata: {
            taskRegex: "task_number_regex",
            owner: "owner",
            presisted: "is_persisted",
            name: "name",
        }
    }
}

export default Object.freeze(constant)